[pytest]
addopts = -v --html=report.html --self-contained-html
testpaths = .
markers =
    crud: booking create/read/update/delete happy paths
    negative: authentication and validation failure cases
